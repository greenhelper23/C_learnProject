#include<stdio.h>
#include <string.h>
#include<stdlib.h>
// stdlib  =  Standard + Library 标准库头文件
/*
函数	作用	格式
malloc	分配 n 字节	void *p = malloc(字节数);
calloc	分配 + 清零	void *p = calloc(元素个数, 每个大小);
realloc	调整大小	void *p = realloc(原指针, 新大小);
free	释放内存	free(指针);
atoi	字符串→int	int x = atoi("123");
atof	字符串→double	double x = atof("3.14");
rand	生成随机数	int x = rand();
srand	设随机种子	srand(time(NULL));
exit	终止程序	exit(0);
abs	int 绝对值	int x = abs(-5);
system	执行系统命令	system("cls");*/


